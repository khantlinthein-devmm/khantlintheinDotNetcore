@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


    <div class="mb-3">
        <label for="exampleFormControlInput1" class="form-label">Name</label>
        <input type="text" class="form-control"  id="Blog_Name" placeholder="Name">
    </div>

    <div class="mb-3">
        <label for="exampleFormControlInput1" class="form-label">Title</label>
        <input type="text" class="form-control"  id="Blog_Title"  placeholder="Title">
    </div>

    <div class="mb-3">
        <label for="exampleFormControlInput1" class="form-label">Content</label>
        <input type="text" class="form-control"  id="Blog_Content" placeholder="Content">
    </div>

    <div class="mb-3">
        <label for="exampleFormControlInput1" class="form-label">Category</label>
        <input type="text" class="form-control"  id="Blog_Category" placeholder="Category">
    </div>

    <a href="/Blog" class="btn btn-dark">Cancel</a>
    <button type="button" id="btnSave" class="btn btn-success">Save</button>

    @section scripts {
    <script>
        $('#btnSave').click(function (e) {
            let title = $('#Blog_Title').val();
            let name = $('#Blog_Name').val();
            let content = $('#Blog_Content').val();
            let category = $('#Blog_Category').val();

            let _requestModel = {
                Blog_Title: title,
                Blog_Name: name,
                Blog_Content: content,
                Blog_Category : category
            };

            console.log({ _requestModel });

            $.ajax({
                type: "POST",
                url: '/BlogAjax/Save',
                data: { requestModel: _requestModel },
                dataType: "json",
                success: function (data) {
                    console.log({ data });
                    alert(data.message);
                    location.href = "/BlogAjax";
                },
                error: function (ex) {
                    var r = jQuery.parseJSON(response.responseText);
                    console.log("Message: " + r.Message);
                    console.log("StackTrace: " + r.StackTrace);
                    console.log("ExceptionType: " + r.ExceptionType);
                }
            });
        })
    </script>
}




